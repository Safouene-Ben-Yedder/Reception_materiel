<form method="post" action="{{ path('appeldoffre_switch_delete', {'id': appeldoffre_switch.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appeldoffre_switch.id) }}">
    <button class="btn">Delete</button>
</form>
