<form method="post" action="{{ path('offredappel_imprimente_delete', {'id': offredappel_imprimente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offredappel_imprimente.id) }}">
    <button class="btn">Delete</button>
</form>
