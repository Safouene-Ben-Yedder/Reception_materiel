<form method="post" action="{{ path('appeldoffre_pcsds_delete', {'id': appeldoffre_pcsd.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appeldoffre_pcsd.id) }}">
    <button class="btn">Delete</button>
</form>
