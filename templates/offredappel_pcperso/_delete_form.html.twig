<form method="post" action="{{ path('offredappel_pcperso_delete', {'id': offredappel_pcperso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offredappel_pcperso.id) }}">
    <button class="btn">Delete</button>
</form>
