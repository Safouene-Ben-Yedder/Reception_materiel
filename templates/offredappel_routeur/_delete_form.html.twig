<form method="post" action="{{ path('offredappel_routeur_delete', {'id': offredappel_routeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offredappel_routeur.id) }}">
    <button class="btn">Delete</button>
</form>
